info:
  name: Get ChartofAccounts By GL Code
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/services/EGF/rest/chartofaccounts?tenantId={{tenantId}}&auth_token={{auth_token}}&glCode=3501000001"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: tenantId
      value: "{{tenantId}}"
      type: query
    - name: auth_token
      value: "{{auth_token}}"
      type: query
    - name: glCode
      value: "3501000001"
      type: query
  body:
    type: json
    data: |-
      {
        "RequestInfo": {
          "apiId": "Finance Data Migration",
          "action": "VIEW",
          "authToken": "{{auth_token}}"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
